<StackLayout>
    <Picker ItemsSource="Entry.Choices.ToList()" Title="Select answer" @bind-SelectedItem="Entry.Selected" >
    </Picker>
</StackLayout>

@code {
    [Parameter]
    public ChoiceEntry Entry { get; set; }
}